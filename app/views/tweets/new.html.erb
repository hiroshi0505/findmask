<div class="contents">
  <div class="scroll">
    <span><h2>[ファイルを選択]から画像を選択し、説明文を入力して下さい</h2></span>
  </div>
  <%= form_with(model: @tweet, local: true) do |form| %>
    <%= render partial: "form", locals: { form: form } %>
  <% end %>
</div>