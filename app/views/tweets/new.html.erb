<div class="contents">
  <div class="scroll">
    <span><h2>投稿ページ</h2></span>
  </div>
  <%= form_with(model: @tweet, local: true) do |form| %>
    <%= render partial: "form", locals: { form: form } %>
  <% end %>
</div>